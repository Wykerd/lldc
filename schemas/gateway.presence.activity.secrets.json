{
    "title": "Activity Secrets",
    "description": "Activity Secrets Structure",
    "type": "object",
    "properties": {
        "join": {
            "description": "OPTIONAL: the secret for joining a party",
            "type": "string"
        },
        "spectate": {
            "description": "OPTIONAL: the secret for spectating a game",
            "type": "string"
        },
        "match": {
            "description": "OPTIONAL: the secret for a specific instanced match",
            "type": "string"
        }
    }
}